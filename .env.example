# Default local mode. Use remote only when testing the deployed proxy.
EXPO_PUBLIC_ANALYZER_MODE=mock
# Base URL only, without /api/analyze-product.
EXPO_PUBLIC_ANALYSIS_API_URL=
# Temporary hackathon abuse deterrent, not real authentication.
EXPO_PUBLIC_ANALYZER_CLIENT_TOKEN=
