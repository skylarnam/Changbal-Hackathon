# Proxy Fixtures

Place temporary local image files here only for manual smoke tests. Do not commit cosmetic label images, user images, or generated smoke output.

The automated proxy tests create synthetic in-memory image bytes and never call Gemini.
